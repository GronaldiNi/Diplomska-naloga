import { FileInfo, FileType } from 'basic-ftp';
import { IFile, IFileType } from '../interfaces';
export declare class FtpUtils {
    /**
     * Formats raw file info.
     * Doesn't include `lastModified`.
     */
    static formatFile(file: FileInfo): IFile;
    static getFileType(type: FileType): IFileType;
    /**
     * Handles ms-dos date and JS date object formats.
     */
    static getValidDate(str: string): Date;
    static getDateFromUnixTime(time: number): Date;
}
