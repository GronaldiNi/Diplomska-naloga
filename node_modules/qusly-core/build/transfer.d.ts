import { ITransferOptions, ITransferProgressListener, ITransferInfo } from './interfaces';
export declare class Transfer {
    info: ITransferInfo;
    options: ITransferOptions;
    onProgress: ITransferProgressListener;
    startTime: number;
    protected bytes: number;
    constructor(info: ITransferInfo, options: ITransferOptions, onProgress: ITransferProgressListener);
    get elapsed(): number;
    get speed(): number;
    get eta(): number;
    get percent(): number;
    handleProgress: (bytes: number) => void;
}
